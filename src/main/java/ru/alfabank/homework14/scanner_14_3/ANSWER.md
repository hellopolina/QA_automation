Разница между getDeclared* и get*:
- в первом случае возвращаются все поля/методы/конструкторы, непосредственно объявленные в самом классе, независимо от модификтора.
- а get* возвращает объявленные public поля/методы/конструкторы + методы класса-родителя Object.